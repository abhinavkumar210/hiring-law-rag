services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      # No network access is needed at runtime: weights are in the image and
      # the index is prebuilt.
      HF_HUB_OFFLINE: "1"
      TRANSFORMERS_OFFLINE: "1"
